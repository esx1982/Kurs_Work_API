Доброго дня!
сначала вызываем функцию:
print(get_token())
чтобы получить токен к VK аккаунту

Далее необходимо получить токен к я.диск и ввести его в 113 строчку кода:
headers = {'Authorization': 'OAuth '}

Затем выполняем остальные функции:
vk_client = VK_get_photo(token, 817676891)
vk_client.get_prof_photos()
vk_client.upload_photos()
vk_client.make_JSON_file()